# oc-image-append
# Autogenerated from man page /usr/lib/jvm/default/man/man1/oc-image-append.1.gz
complete -c oc-image-append -l created-at -d '    The creation date for this image, in RFC3339 format or milliseconds from …'
complete -c oc-image-append -l dir -d '    The directory on disk that file:// images will be copied under'
complete -c oc-image-append -l drop-history -d '    Fields on the image that relate to the history of how the image was creat…'
complete -c oc-image-append -l dry-run -d '    Print the actions that would be taken and exit without writing to the des…'
complete -c oc-image-append -l filter-by-os -d '    A regular expression to control which images are considered when multiple…'
complete -c oc-image-append -l force -d '    If set, the command will attempt to upload all layers instead of skipping…'
complete -c oc-image-append -l from -d '    The image to use as a base.  If empty, a new scratch image is created'
complete -c oc-image-append -l from-dir -d '    The directory on disk that file:// images will be read from'
complete -c oc-image-append -l image -d '    A JSON patch that will be used with the output image data'
complete -c oc-image-append -l insecure -d '    Allow push and pull operations to registries to be made over HTTP'
complete -c oc-image-append -l max-per-registry -d '    Number of concurrent requests allowed per registry'
complete -c oc-image-append -l meta -d '    A JSON patch that will be used with image base metadata (advanced config)'
complete -c oc-image-append -s a -l registry-config -d '    Path to your registry credentials (defaults to  /. docker/config. json)'
complete -c oc-image-append -l skip-verification -d '    Skip verifying the integrity of the retrieved content'
complete -c oc-image-append -l to -d '    The Docker repository tag to upload the appended image to'
complete -c oc-image-append -l as -d '    Username to impersonate for the operation'
complete -c oc-image-append -l as-group -d '    Group to impersonate for the operation, this flag can be repeated to spec…'
complete -c oc-image-append -l cache-dir -d '    Default cache directory'
complete -c oc-image-append -l certificate-authority -d '    Path to a cert file for the certificate authority'
complete -c oc-image-append -l client-certificate -d '    Path to a client certificate file for TLS'
complete -c oc-image-append -l client-key -d '    Path to a client key file for TLS'
complete -c oc-image-append -l cluster -d '    The name of the kubeconfig cluster to use'
complete -c oc-image-append -l context -d '    The name of the kubeconfig context to use'
complete -c oc-image-append -l insecure-skip-tls-verify -d '    If true, the server\'s certificate will not be checked for validity'
complete -c oc-image-append -l kubeconfig -d '    Path to the kubeconfig file to use for CLI requests'
complete -c oc-image-append -l match-server-version -d '    Require server version to match client version'
complete -c oc-image-append -s n -l namespace -d '    If present, the namespace scope for this CLI request'
complete -c oc-image-append -l request-timeout -d '    The length of time to wait before giving up on a single server request'
complete -c oc-image-append -s s -l server -d '    The address and port of the Kubernetes API server'
complete -c oc-image-append -l tls-server-name -d '    Server name to use for server certificate validation'
complete -c oc-image-append -l token -d '    Bearer token for authentication to the API server'
complete -c oc-image-append -l user -d '    The name of the kubeconfig user to use EXAMPLE   # Remove the entrypoint …'

