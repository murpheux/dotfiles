# oc-ex-dockergc
# Autogenerated from man page /usr/lib/jvm/default/man/man1/oc-ex-dockergc.1.gz
complete -c oc-ex-dockergc -l dry-run -d '    Run in single-pass mode with no effect'
complete -c oc-ex-dockergc -l image-gc-high-threshold -d '    The percent of disk usage after which image garbage collection is always …'
complete -c oc-ex-dockergc -l image-gc-low-threshold -d '    The percent of disk usage before which image garbage collection is never …'
complete -c oc-ex-dockergc -l minimum-ttl-duration -d '    Minimum age for a container or unused image before it is garbage collected'
complete -c oc-ex-dockergc -l as -d '    Username to impersonate for the operation'
complete -c oc-ex-dockergc -l as-group -d '    Group to impersonate for the operation, this flag can be repeated to spec…'
complete -c oc-ex-dockergc -l cache-dir -d '    Default cache directory'
complete -c oc-ex-dockergc -l certificate-authority -d '    Path to a cert file for the certificate authority'
complete -c oc-ex-dockergc -l client-certificate -d '    Path to a client certificate file for TLS'
complete -c oc-ex-dockergc -l client-key -d '    Path to a client key file for TLS'
complete -c oc-ex-dockergc -l cluster -d '    The name of the kubeconfig cluster to use'
complete -c oc-ex-dockergc -l context -d '    The name of the kubeconfig context to use'
complete -c oc-ex-dockergc -l insecure-skip-tls-verify -d '    If true, the server\'s certificate will not be checked for validity'
complete -c oc-ex-dockergc -l kubeconfig -d '    Path to the kubeconfig file to use for CLI requests'
complete -c oc-ex-dockergc -l match-server-version -d '    Require server version to match client version'
complete -c oc-ex-dockergc -s n -l namespace -d '    If present, the namespace scope for this CLI request'
complete -c oc-ex-dockergc -l request-timeout -d '    The length of time to wait before giving up on a single server request'
complete -c oc-ex-dockergc -s s -l server -d '    The address and port of the Kubernetes API server'
complete -c oc-ex-dockergc -l tls-server-name -d '    Server name to use for server certificate validation'
complete -c oc-ex-dockergc -l token -d '    Bearer token for authentication to the API server'
complete -c oc-ex-dockergc -l user -d '    The name of the kubeconfig user to use EXAMPLE   # Perform garbage collec…'

