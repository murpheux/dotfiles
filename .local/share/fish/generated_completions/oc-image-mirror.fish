# oc-image-mirror
# Autogenerated from man page /usr/lib/jvm/default/man/man1/oc-image-mirror.1.gz
complete -c oc-image-mirror -l continue-on-error -d '    If an error occurs, keep going and attempt to mirror as much as possible'
complete -c oc-image-mirror -l dir -d '    The directory on disk that file:// images will be copied under'
complete -c oc-image-mirror -l dry-run -d '    Print the actions that would be taken and exit without writing to the des…'
complete -c oc-image-mirror -s f -l filename -d '    One or more files to read SRC=DST or SRC DST [DST . ] mappings from'
complete -c oc-image-mirror -l filter-by-os -d '    A regular expression to control which images are considered when multiple…'
complete -c oc-image-mirror -l force -d '    Attempt to write all layers and manifests even if they exist in the remot…'
complete -c oc-image-mirror -l from-dir -d '    The directory on disk that file:// images will be read from'
complete -c oc-image-mirror -l insecure -d '    Allow push and pull operations to registries to be made over HTTP'
complete -c oc-image-mirror -l keep-manifest-list -d '    If an image is part of a manifest list, always mirror the list even if on…'
complete -c oc-image-mirror -l max-per-registry -d '    Number of concurrent requests allowed per registry'
complete -c oc-image-mirror -l max-registry -d '    Number of concurrent registries to connect to at any one time'
complete -c oc-image-mirror -s a -l registry-config -d '    Path to your registry credentials (defaults to  /. docker/config. json)'
complete -c oc-image-mirror -l s3-source-bucket -d '    A list of bucket/path locations on S3 that may contain already uploaded b…'
complete -c oc-image-mirror -l skip-missing -d '    If an input image is not found, skip them'
complete -c oc-image-mirror -l skip-mount -d '    Always push layers instead of cross-mounting them'
complete -c oc-image-mirror -l skip-multiple-scopes -d '    Some registries do not support multiple scopes passed to the registry log…'
complete -c oc-image-mirror -l skip-verification -d '    Skip verifying the integrity of the retrieved content'
complete -c oc-image-mirror -l as -d '    Username to impersonate for the operation'
complete -c oc-image-mirror -l as-group -d '    Group to impersonate for the operation, this flag can be repeated to spec…'
complete -c oc-image-mirror -l cache-dir -d '    Default cache directory'
complete -c oc-image-mirror -l certificate-authority -d '    Path to a cert file for the certificate authority'
complete -c oc-image-mirror -l client-certificate -d '    Path to a client certificate file for TLS'
complete -c oc-image-mirror -l client-key -d '    Path to a client key file for TLS'
complete -c oc-image-mirror -l cluster -d '    The name of the kubeconfig cluster to use'
complete -c oc-image-mirror -l context -d '    The name of the kubeconfig context to use'
complete -c oc-image-mirror -l insecure-skip-tls-verify -d '    If true, the server\'s certificate will not be checked for validity'
complete -c oc-image-mirror -l kubeconfig -d '    Path to the kubeconfig file to use for CLI requests'
complete -c oc-image-mirror -l match-server-version -d '    Require server version to match client version'
complete -c oc-image-mirror -s n -l namespace -d '    If present, the namespace scope for this CLI request'
complete -c oc-image-mirror -l request-timeout -d '    The length of time to wait before giving up on a single server request'
complete -c oc-image-mirror -s s -l server -d '    The address and port of the Kubernetes API server'
complete -c oc-image-mirror -l tls-server-name -d '    Server name to use for server certificate validation'
complete -c oc-image-mirror -l token -d '    Bearer token for authentication to the API server'
complete -c oc-image-mirror -l user -d '    The name of the kubeconfig user to use EXAMPLE   # Copy image to another …'

