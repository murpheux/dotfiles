# oc-set-env
# Autogenerated from man page /usr/lib/jvm/default/man/man1/oc-set-env.1.gz
complete -c oc-set-env -l all -d '    If true, select all resources in the namespace of the specified resource …'
complete -c oc-set-env -l allow-missing-template-keys -d '    If true, ignore any errors in templates when a field or map key is missin…'
complete -c oc-set-env -s c -l containers -d '    The names of containers in the selected pod templates to change - may use…'
complete -c oc-set-env -l dry-run -d '    Must be "none", "server", or "client"'
complete -c oc-set-env -s e -l env -d '    Specify a key-value pair for an environment variable to set into each con…'
complete -c oc-set-env -s f -l filename -d '    Filename, directory, or URL to files to use to edit the resource'
complete -c oc-set-env -l from -d '    The name of a resource from which to inject environment variables'
complete -c oc-set-env -s k -l kustomize -d '    Process the kustomization directory'
complete -c oc-set-env -l list -d '    If true, display the environment and any changes in the standard format'
complete -c oc-set-env -l local -d '    If true, set image will NOT contact api-server but run locally'
complete -c oc-set-env -s o -l output -d '    Output format'
complete -c oc-set-env -l overwrite -d '    If true, allow environment to be overwritten, otherwise reject updates th…'
complete -c oc-set-env -l prefix -d '    Prefix to append to variable names'
complete -c oc-set-env -s R -l recursive -d '    Process the directory used in -f, --filename recursively'
complete -c oc-set-env -l resolve -d '    If true, show secret or configmap references when listing variables'
complete -c oc-set-env -l resource-version -d '    If non-empty, the labels update will only succeed if this is the current …'
complete -c oc-set-env -s l -l selector -d '    Selector (label query) to filter on'
complete -c oc-set-env -l template -d '    Template string or path to template file to use when -o=go-template, -o=g…'
complete -c oc-set-env -l as -d '    Username to impersonate for the operation'
complete -c oc-set-env -l as-group -d '    Group to impersonate for the operation, this flag can be repeated to spec…'
complete -c oc-set-env -l cache-dir -d '    Default cache directory'
complete -c oc-set-env -l certificate-authority -d '    Path to a cert file for the certificate authority'
complete -c oc-set-env -l client-certificate -d '    Path to a client certificate file for TLS'
complete -c oc-set-env -l client-key -d '    Path to a client key file for TLS'
complete -c oc-set-env -l cluster -d '    The name of the kubeconfig cluster to use'
complete -c oc-set-env -l context -d '    The name of the kubeconfig context to use'
complete -c oc-set-env -l insecure-skip-tls-verify -d '    If true, the server\'s certificate will not be checked for validity'
complete -c oc-set-env -l kubeconfig -d '    Path to the kubeconfig file to use for CLI requests'
complete -c oc-set-env -l match-server-version -d '    Require server version to match client version'
complete -c oc-set-env -s n -l namespace -d '    If present, the namespace scope for this CLI request'
complete -c oc-set-env -l request-timeout -d '    The length of time to wait before giving up on a single server request'
complete -c oc-set-env -s s -l server -d '    The address and port of the Kubernetes API server'
complete -c oc-set-env -l tls-server-name -d '    Server name to use for server certificate validation'
complete -c oc-set-env -l token -d '    Bearer token for authentication to the API server'
complete -c oc-set-env -l user -d '    The name of the kubeconfig user to use EXAMPLE   # Update deployment conf…'

