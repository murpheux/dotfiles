# oc-adm-must-gather
# Autogenerated from man page /usr/lib/jvm/default/man/man1/oc-adm-must-gather.1.gz
complete -c oc-adm-must-gather -l dest-dir -d '    Set a specific directory on the local machine to write gathered data to'
complete -c oc-adm-must-gather -l image -d '    Specify a must-gather plugin image to run'
complete -c oc-adm-must-gather -l image-stream -d '    Specify an image stream (namespace/name:tag) containing a must-gather plu…'
complete -c oc-adm-must-gather -l node-name -d '    Set a specific node to use - by default a random master will be used'
complete -c oc-adm-must-gather -l source-dir -d '    Set the specific directory on the pod copy the gathered data from'
complete -c oc-adm-must-gather -l timeout -d '    The length of time to gather data, in seconds.  Defaults to 10 minutes'
complete -c oc-adm-must-gather -l as -d '    Username to impersonate for the operation'
complete -c oc-adm-must-gather -l as-group -d '    Group to impersonate for the operation, this flag can be repeated to spec…'
complete -c oc-adm-must-gather -l cache-dir -d '    Default cache directory'
complete -c oc-adm-must-gather -l certificate-authority -d '    Path to a cert file for the certificate authority'
complete -c oc-adm-must-gather -l client-certificate -d '    Path to a client certificate file for TLS'
complete -c oc-adm-must-gather -l client-key -d '    Path to a client key file for TLS'
complete -c oc-adm-must-gather -l cluster -d '    The name of the kubeconfig cluster to use'
complete -c oc-adm-must-gather -l context -d '    The name of the kubeconfig context to use'
complete -c oc-adm-must-gather -l insecure-skip-tls-verify -d '    If true, the server\'s certificate will not be checked for validity'
complete -c oc-adm-must-gather -l kubeconfig -d '    Path to the kubeconfig file to use for CLI requests'
complete -c oc-adm-must-gather -l match-server-version -d '    Require server version to match client version'
complete -c oc-adm-must-gather -s n -l namespace -d '    If present, the namespace scope for this CLI request'
complete -c oc-adm-must-gather -l request-timeout -d '    The length of time to wait before giving up on a single server request'
complete -c oc-adm-must-gather -s s -l server -d '    The address and port of the Kubernetes API server'
complete -c oc-adm-must-gather -l tls-server-name -d '    Server name to use for server certificate validation'
complete -c oc-adm-must-gather -l token -d '    Bearer token for authentication to the API server'
complete -c oc-adm-must-gather -l user -d '    The name of the kubeconfig user to use EXAMPLE   # gather information usi…'

