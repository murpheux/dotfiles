# oc-auth-reconcile
# Autogenerated from man page /usr/lib/jvm/default/man/man1/oc-auth-reconcile.1.gz
complete -c oc-auth-reconcile -l allow-missing-template-keys -d '    If true, ignore any errors in templates when a field or map key is missin…'
complete -c oc-auth-reconcile -l dry-run -d '    Must be "none", "server", or "client"'
complete -c oc-auth-reconcile -s f -l filename -d '    Filename, directory, or URL to files identifying the resource to reconcile'
complete -c oc-auth-reconcile -s k -l kustomize -d '    Process the kustomization directory'
complete -c oc-auth-reconcile -s o -l output -d '    Output format'
complete -c oc-auth-reconcile -s R -l recursive -d '    Process the directory used in -f, --filename recursively'
complete -c oc-auth-reconcile -l remove-extra-permissions -d '    If true, removes extra permissions added to roles'
complete -c oc-auth-reconcile -l remove-extra-subjects -d '    If true, removes extra subjects added to rolebindings'
complete -c oc-auth-reconcile -l template -d '    Template string or path to template file to use when -o=go-template, -o=g…'
complete -c oc-auth-reconcile -l as -d '    Username to impersonate for the operation'
complete -c oc-auth-reconcile -l as-group -d '    Group to impersonate for the operation, this flag can be repeated to spec…'
complete -c oc-auth-reconcile -l cache-dir -d '    Default cache directory'
complete -c oc-auth-reconcile -l certificate-authority -d '    Path to a cert file for the certificate authority'
complete -c oc-auth-reconcile -l client-certificate -d '    Path to a client certificate file for TLS'
complete -c oc-auth-reconcile -l client-key -d '    Path to a client key file for TLS'
complete -c oc-auth-reconcile -l cluster -d '    The name of the kubeconfig cluster to use'
complete -c oc-auth-reconcile -l context -d '    The name of the kubeconfig context to use'
complete -c oc-auth-reconcile -l insecure-skip-tls-verify -d '    If true, the server\'s certificate will not be checked for validity'
complete -c oc-auth-reconcile -l kubeconfig -d '    Path to the kubeconfig file to use for CLI requests'
complete -c oc-auth-reconcile -l match-server-version -d '    Require server version to match client version'
complete -c oc-auth-reconcile -s n -l namespace -d '    If present, the namespace scope for this CLI request'
complete -c oc-auth-reconcile -l request-timeout -d '    The length of time to wait before giving up on a single server request'
complete -c oc-auth-reconcile -s s -l server -d '    The address and port of the Kubernetes API server'
complete -c oc-auth-reconcile -l tls-server-name -d '    Server name to use for server certificate validation'
complete -c oc-auth-reconcile -l token -d '    Bearer token for authentication to the API server'
complete -c oc-auth-reconcile -l user -d '    The name of the kubeconfig user to use EXAMPLE   # Reconcile rbac resourc…'

