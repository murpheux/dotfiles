# oc-autoscale
# Autogenerated from man page /usr/lib/jvm/default/man/man1/oc-autoscale.1.gz
complete -c oc-autoscale -l allow-missing-template-keys -d '    If true, ignore any errors in templates when a field or map key is missin…'
complete -c oc-autoscale -l cpu-percent -s 1 -d '    The target average CPU utilization (represented as a percent of requested…'
complete -c oc-autoscale -l dry-run -d '    Must be "none", "server", or "client"'
complete -c oc-autoscale -l field-manager -d '    Name of the manager used to track field ownership'
complete -c oc-autoscale -s f -l filename -d '    Filename, directory, or URL to files identifying the resource to autoscale'
complete -c oc-autoscale -l generator -d '    The name of the API generator to use'
complete -c oc-autoscale -s k -l kustomize -d '    Process the kustomization directory'
complete -c oc-autoscale -l max -d '    The upper limit for the number of pods that can be set by the autoscaler'
complete -c oc-autoscale -l min -d '    The lower limit for the number of pods that can be set by the autoscaler'
complete -c oc-autoscale -l name -d '    The name for the newly created object'
complete -c oc-autoscale -s o -l output -d '    Output format'
complete -c oc-autoscale -l record -d '    Record current kubectl command in the resource annotation'
complete -c oc-autoscale -s R -l recursive -d '    Process the directory used in -f, --filename recursively'
complete -c oc-autoscale -l save-config -d '    If true, the configuration of current object will be saved in its annotat…'
complete -c oc-autoscale -l template -d '    Template string or path to template file to use when -o=go-template, -o=g…'
complete -c oc-autoscale -l as -d '    Username to impersonate for the operation'
complete -c oc-autoscale -l as-group -d '    Group to impersonate for the operation, this flag can be repeated to spec…'
complete -c oc-autoscale -l cache-dir -d '    Default cache directory'
complete -c oc-autoscale -l certificate-authority -d '    Path to a cert file for the certificate authority'
complete -c oc-autoscale -l client-certificate -d '    Path to a client certificate file for TLS'
complete -c oc-autoscale -l client-key -d '    Path to a client key file for TLS'
complete -c oc-autoscale -l cluster -d '    The name of the kubeconfig cluster to use'
complete -c oc-autoscale -l context -d '    The name of the kubeconfig context to use'
complete -c oc-autoscale -l insecure-skip-tls-verify -d '    If true, the server\'s certificate will not be checked for validity'
complete -c oc-autoscale -l kubeconfig -d '    Path to the kubeconfig file to use for CLI requests'
complete -c oc-autoscale -l match-server-version -d '    Require server version to match client version'
complete -c oc-autoscale -s n -l namespace -d '    If present, the namespace scope for this CLI request'
complete -c oc-autoscale -l request-timeout -d '    The length of time to wait before giving up on a single server request'
complete -c oc-autoscale -s s -l server -d '    The address and port of the Kubernetes API server'
complete -c oc-autoscale -l tls-server-name -d '    Server name to use for server certificate validation'
complete -c oc-autoscale -l token -d '    Bearer token for authentication to the API server'
complete -c oc-autoscale -l user -d '    The name of the kubeconfig user to use EXAMPLE   # Auto scale a deploymen…'

