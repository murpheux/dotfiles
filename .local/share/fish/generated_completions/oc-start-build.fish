# oc-start-build
# Autogenerated from man page /usr/lib/jvm/default/man/man1/oc-start-build.1.gz
complete -c oc-start-build -l allow-missing-template-keys -d '    If true, ignore any errors in templates when a field or map key is missin…'
complete -c oc-start-build -l build-arg -d '    Specify a key-value pair to pass to Docker during the build'
complete -c oc-start-build -l build-loglevel -d '    Specify the log level for the build log output'
complete -c oc-start-build -l commit -d '    Specify the source code commit identifier the build should use; requires …'
complete -c oc-start-build -s e -l env -d '    Specify a key-value pair for an environment variable to set for the build…'
complete -c oc-start-build -l exclude -d '    When using the --from-dir option: regular expression for selecting files …'
complete -c oc-start-build -s F -l follow -d '    Start a build and watch its logs until it completes or fails'
complete -c oc-start-build -l from-archive -d '    An archive (tar, tar'
complete -c oc-start-build -l from-build -d '    Specify the name of a build which should be re-run'
complete -c oc-start-build -l from-dir -d '    A directory to archive and use as the binary input for a build'
complete -c oc-start-build -l from-file -d '    A file to use as the binary input for the build; example a pom'
complete -c oc-start-build -l from-repo -d '    The path to a local source code repository to use as the binary input for…'
complete -c oc-start-build -l from-webhook -d '    Specify a generic webhook URL for an existing build config to trigger'
complete -c oc-start-build -l git-post-receive -d '    The contents of the post-receive hook to trigger a build'
complete -c oc-start-build -l git-repository -d '    The path to the git repository for post-receive; defaults to the current …'
complete -c oc-start-build -l incremental -d '    Overrides the incremental setting in a source-strategy build, ignored if …'
complete -c oc-start-build -l list-webhooks -d '    List the webhooks for the specified build config or build; accepts \'all\',…'
complete -c oc-start-build -l no-cache -d '    Overrides the noCache setting in a docker-strategy build, ignored if not …'
complete -c oc-start-build -s o -l output -d '    Output format'
complete -c oc-start-build -l template -d '    Template string or path to template file to use when -o=go-template, -o=g…'
complete -c oc-start-build -s w -l wait -d '    Wait for a build to complete and exit with a non-zero return code if the …'
complete -c oc-start-build -l as -d '    Username to impersonate for the operation'
complete -c oc-start-build -l as-group -d '    Group to impersonate for the operation, this flag can be repeated to spec…'
complete -c oc-start-build -l cache-dir -d '    Default cache directory'
complete -c oc-start-build -l certificate-authority -d '    Path to a cert file for the certificate authority'
complete -c oc-start-build -l client-certificate -d '    Path to a client certificate file for TLS'
complete -c oc-start-build -l client-key -d '    Path to a client key file for TLS'
complete -c oc-start-build -l cluster -d '    The name of the kubeconfig cluster to use'
complete -c oc-start-build -l context -d '    The name of the kubeconfig context to use'
complete -c oc-start-build -l insecure-skip-tls-verify -d '    If true, the server\'s certificate will not be checked for validity'
complete -c oc-start-build -l kubeconfig -d '    Path to the kubeconfig file to use for CLI requests'
complete -c oc-start-build -l match-server-version -d '    Require server version to match client version'
complete -c oc-start-build -s n -l namespace -d '    If present, the namespace scope for this CLI request'
complete -c oc-start-build -l request-timeout -d '    The length of time to wait before giving up on a single server request'
complete -c oc-start-build -s s -l server -d '    The address and port of the Kubernetes API server'
complete -c oc-start-build -l tls-server-name -d '    Server name to use for server certificate validation'
complete -c oc-start-build -l token -d '    Bearer token for authentication to the API server'
complete -c oc-start-build -l user -d '    The name of the kubeconfig user to use EXAMPLE   # Starts build from buil…'

