# oc-debug
# Autogenerated from man page /usr/lib/jvm/default/man/man1/oc-debug.1.gz
complete -c oc-debug -l allow-missing-template-keys -d '    If true, ignore any errors in templates when a field or map key is missin…'
complete -c oc-debug -l as-root -d '    If true, try to run the container as the root user'
complete -c oc-debug -l as-user -s 1 -d '    Try to run the container as a specific user UID (note: admins may limit y…'
complete -c oc-debug -s c -l container -d '    Container name; defaults to first container'
complete -c oc-debug -l dry-run -d '    Must be "none", "server", or "client"'
complete -c oc-debug -s f -l filename -d '    Filename, directory, or URL to files to read a template'
complete -c oc-debug -l image -d '    Override the image used by the targeted container'
complete -c oc-debug -l image-stream -d '    Specify an image stream (namespace/name:tag) containing a debug image to …'
complete -c oc-debug -l keep-annotations -d '    If true, keep the original pod annotations'
complete -c oc-debug -l keep-init-containers -d '    Run the init containers for the pod.  Defaults to true'
complete -c oc-debug -l keep-liveness -d '    If true, keep the original pod liveness probes'
complete -c oc-debug -l keep-readiness -d '    If true, keep the original pod readiness probes'
complete -c oc-debug -s k -l kustomize -d '    Process the kustomization directory'
complete -c oc-debug -s I -l no-stdin -d '    Bypasses passing STDIN to the container, defaults to true if no command s…'
complete -c oc-debug -s T -l no-tty -d '    Disable pseudo-terminal allocation'
complete -c oc-debug -l node-name -d '    Set a specific node to run on - by default the pod will run on any valid …'
complete -c oc-debug -l one-container -d '    If true, run only the selected container, remove all others'
complete -c oc-debug -s o -l output -d '    Output format'
complete -c oc-debug -l preserve-pod -d '    If true, the pod will not be deleted after the debug command exits'
complete -c oc-debug -s q -l quiet -d '    No informational messages will be printed'
complete -c oc-debug -s R -l recursive -d '    Process the directory used in -f, --filename recursively'
complete -c oc-debug -l show-labels -d '    When printing, show all labels as the last column (default hide labels co…'
complete -c oc-debug -l template -d '    Template string or path to template file to use when -o=go-template, -o=g…'
complete -c oc-debug -l to-namespace -d '    Override the namespace to create the pod into (instead of using --namespa…'
complete -c oc-debug -s t -l tty -d '    Force a pseudo-terminal to be allocated OPTIONS INHERITED FROM PARENT COM…'
complete -c oc-debug -l as -d '    Username to impersonate for the operation'
complete -c oc-debug -l as-group -d '    Group to impersonate for the operation, this flag can be repeated to spec…'
complete -c oc-debug -l cache-dir -d '    Default cache directory'
complete -c oc-debug -l certificate-authority -d '    Path to a cert file for the certificate authority'
complete -c oc-debug -l client-certificate -d '    Path to a client certificate file for TLS'
complete -c oc-debug -l client-key -d '    Path to a client key file for TLS'
complete -c oc-debug -l cluster -d '    The name of the kubeconfig cluster to use'
complete -c oc-debug -l context -d '    The name of the kubeconfig context to use'
complete -c oc-debug -l insecure-skip-tls-verify -d '    If true, the server\'s certificate will not be checked for validity'
complete -c oc-debug -l kubeconfig -d '    Path to the kubeconfig file to use for CLI requests'
complete -c oc-debug -l match-server-version -d '    Require server version to match client version'
complete -c oc-debug -s n -l namespace -d '    If present, the namespace scope for this CLI request'
complete -c oc-debug -l request-timeout -d '    The length of time to wait before giving up on a single server request'
complete -c oc-debug -s s -l server -d '    The address and port of the Kubernetes API server'
complete -c oc-debug -l tls-server-name -d '    Server name to use for server certificate validation'
complete -c oc-debug -l token -d '    Bearer token for authentication to the API server'
complete -c oc-debug -l user -d '    The name of the kubeconfig user to use EXAMPLE   # Debug a currently runn…'

