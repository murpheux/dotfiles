# oc-image-extract
# Autogenerated from man page /usr/lib/jvm/default/man/man1/oc-image-extract.1.gz
complete -c oc-image-extract -l all-layers -d '    For dry-run mode, process from lowest to highest layer and don\'t omit dup…'
complete -c oc-image-extract -l confirm -d '    Pass to allow extracting to non-empty directories'
complete -c oc-image-extract -l dir -d '    The directory on disk that file:// images will be copied under'
complete -c oc-image-extract -l dry-run -d '    Print the actions that would be taken and exit without writing any conten…'
complete -c oc-image-extract -l file -d '    Extract the specified files to the current directory'
complete -c oc-image-extract -l filter-by-os -d '    A regular expression to control which images are considered when multiple…'
complete -c oc-image-extract -l insecure -d '    Allow push and pull operations to registries to be made over HTTP'
complete -c oc-image-extract -l only-files -d '    Only extract regular files and directories from the image'
complete -c oc-image-extract -l path -d '    Extract only part of an image'
complete -c oc-image-extract -s p -l preserve-ownership -d '    Preserve the permissions of extracted files'
complete -c oc-image-extract -s a -l registry-config -d '    Path to your registry credentials (defaults to  /. docker/config. json)'
complete -c oc-image-extract -l skip-verification -d '    Skip verifying the integrity of the retrieved content'
complete -c oc-image-extract -l as -d '    Username to impersonate for the operation'
complete -c oc-image-extract -l as-group -d '    Group to impersonate for the operation, this flag can be repeated to spec…'
complete -c oc-image-extract -l cache-dir -d '    Default cache directory'
complete -c oc-image-extract -l certificate-authority -d '    Path to a cert file for the certificate authority'
complete -c oc-image-extract -l client-certificate -d '    Path to a client certificate file for TLS'
complete -c oc-image-extract -l client-key -d '    Path to a client key file for TLS'
complete -c oc-image-extract -l cluster -d '    The name of the kubeconfig cluster to use'
complete -c oc-image-extract -l context -d '    The name of the kubeconfig context to use'
complete -c oc-image-extract -l insecure-skip-tls-verify -d '    If true, the server\'s certificate will not be checked for validity'
complete -c oc-image-extract -l kubeconfig -d '    Path to the kubeconfig file to use for CLI requests'
complete -c oc-image-extract -l match-server-version -d '    Require server version to match client version'
complete -c oc-image-extract -s n -l namespace -d '    If present, the namespace scope for this CLI request'
complete -c oc-image-extract -l request-timeout -d '    The length of time to wait before giving up on a single server request'
complete -c oc-image-extract -s s -l server -d '    The address and port of the Kubernetes API server'
complete -c oc-image-extract -l tls-server-name -d '    Server name to use for server certificate validation'
complete -c oc-image-extract -l token -d '    Bearer token for authentication to the API server'
complete -c oc-image-extract -l user -d '    The name of the kubeconfig user to use EXAMPLE   # Extract the busybox im…'

